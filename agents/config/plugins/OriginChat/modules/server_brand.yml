# ServerBrand module configuration
config-version: 1

# Enable/disable the module
enabled: true

# Server core name to be displayed in the F3 menu
# Supports color codes (&f, &f, &f, etc.) and HEX colors (#RRGGBB)
server_brand: '&fAgiCraft'

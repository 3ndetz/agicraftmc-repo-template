config-version = "2.7"
bind = "0.0.0.0:25565"
motd = "<bold><aqua>Agi<gray>-<light_purple>Craft </light_purple></bold><yellow>⪼</yellow> <gradient:light_purple:#ffffff>Заходи с версии</gradient> <white>1.21</white><gradient:white:aqua><newline>ОКАК <black>/otpor</black>"
show-max-players = 5090
# Online mode like in other servers
online-mode = false
# For online mode checking client brand
force-key-authentication = false
# Modern forward mode (more secure, need to specify keys in paper-global.yml)
# https://docs.papermc.io/velocity/player-information-forwarding/
player-info-forwarding-mode = "modern"
forwarding-secret-file = "forwarding.secret"

[servers]
# network_mode: host — connect via localhost ports bound on 127.0.0.1
agents = "127.0.0.1:25576"
clown = "127.0.0.1:25576"
nanolimbo= "127.0.0.1:25579"
lobby = "127.0.0.1:25575"
survival = "127.0.0.1:25571"
limbo = "127.0.0.1:25578"
private = "127.0.0.1:25574"
# survival_plus = "127.0.0.1:25573"  # Coming soon

# Main default server for new players
default = "clown"

# Global fallback server
fallback = "nanolimbo"

# Sequence of attempts
try = ["default", "nanolimbo", "lobby", "survival"]

[forced-hosts]
"example.com" = ["agents", "nanolimbo"]
"mc.example.com" = ["agents", "nanolimbo"]
"agents.example.com" = ["agents", "nanolimbo"]
"survival.example.com" = ["survival", "nanolimbo"]
"limbo.example.com" = ["nanolimbo"]
"private.example.com" = ["private", "nanolimbo"]

[advanced]
compression-threshold = 256
compression-level = -1
login-ratelimit = 3000
connection-timeout = 5000
# On Linux: false (direct port mapping preserves real IPs without proxy)
# On Windows with gobetween/nginx: set to true
haproxy-protocol = false

[query]
# If query is enabled, on what port should the query protocol listen on?
port = 25565
show-plugins = true
# This is the map name that is reported to the query services.
map = "dust2"
# Whether to enable responding to GameSpy 4 query responses or not.
enabled = true
